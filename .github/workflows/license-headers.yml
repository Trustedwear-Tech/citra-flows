# Copyright (c) 2026 Trustedwear Tech Private Limited (https://citra-ai.com)
# Author: Rohit Kumar Chandan
# SPDX-License-Identifier: Apache-2.0
#
# Licensed under the Apache License, Version 2.0 (the "License"); you may not
# use this file except in compliance with the License. You may obtain a copy of
# the License at http://www.apache.org/licenses/LICENSE-2.0

# Guard: every tracked source file carries the Apache-2.0 notice.
#
# Borrowed from citra-decision-system and citra-decks, which use the same
# licence tooling. The scope comes from `git ls-files`, so this never reaches
# vendored or third-party code -- the citra-common submodule (Apache-2.0) is a
# gitlink, never a file, and scripts/license_headers.py excludes it besides.

name: license headers

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  headers:
    name: Apache-2.0 header on every tracked source file
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Exits 1 and names the files if any are missing.
      - name: Check licence headers
        run: python scripts/license_headers.py --check

      - name: Unit tests for the licence tooling
        run: |
          pip install pytest pyyaml
          python -m pytest scripts/test_license_headers.py \
                           -q
